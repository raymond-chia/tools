# 專案協作規則

## 核心原則

- 所有技術決策以易於維護為第一優先，不為未指定的未來需求預作設計。
- 當使用者已清楚描述可見行為時，不要求使用者決定純技術細節；依現有專案慣例選擇最小且易維護的做法。
- 程式碼註解、文件說明與協作回覆優先使用繁體中文。

## 修改流程

### 可維護性問題

若現有實作搭配目前已確定的新功能會變得不易維護，應主動調整相關結構與作法，不受原始實作限制。調整仍須限於目前需求，並在回覆中簡述問題、影響與實際採取的方向。

### 先檢查既有慣例

新增或修改檔案前，必須先查看同一用途的既有檔案及其直接相關的共用檔案，並依現有慣例決定檔案位置、命名、資料放置與測試寫法；不得只依一般經驗自行決定。

- 新增或修改資料格式、資料檔前，先查看既有同類資料檔、schema、載入呼叫點，以及編輯器與執行期的使用位置。
- 使用者明確要求撰寫或修改測試時，先查看目標測試模組、同層至少一個同類測試檔，以及共用 fixture／constants 檔。
- 若既有慣例已涵蓋目前情境，直接遵循；只有現有結構無法涵蓋時，才提出方案供使用者確認。

### 實作前確認

只向使用者確認規格、使用者可見行為與操作方式。API、資料格式、架構、檔案範圍、相容性及效能等純技術選擇，由實作者依現有專案慣例自行選擇最容易持續維護的做法。

當規格或使用者可見行為存在兩種以上合理解讀，且現有專案內容無法判定時，修改檔案前必須：

1. 先檢查現有專案結構、慣例與相關檔案。
2. 只列出需要使用者決定的規格選項及其可見差異。
3. 等使用者確認規格後再修改檔案。

若規格與可見行為已清楚，直接實作，不為純技術細節要求確認。實作期間持續維護受影響的既有結構；發現原作法不適合新功能時，應一併改為較容易維護的做法。

### 外部查詢與技術依據

當需求涉及可能變動、版本相依、引擎或框架行為，或對既有行為沒有把握時，實作前必須查詢官方文件或其他第一方資料，不得只依記憶推測。

查詢結果只用於當次任務的判斷，不要把整份外部文件或逐次查詢內容複製到本檔案。完成任務時，只在回覆中簡述真正影響實作的依據與來源；不要在 `AGENTS.md` 累積查詢紀錄或固定過時版本結論。

## 實作範圍

- 不要為未指定的未來需求增加相容性、格式支援或防禦性分支。
- 只依照目前明確定義的格式與行為實作。

### 測試

- 除非使用者明確要求，否則禁止撰寫或修改測試。
- 每個測試案例都必須以一行註解簡短說明其測試行為；修改測試時，也必須同步更新該說明。
- 測試只能載入位於測試目錄內的專用測試資料；禁止使用正式遊戲、範例或其他非測試資料。

### 驗證責任

- 驗證應放在資料的作者輸入邊界；不要在已信任資料的下游流程重複驗證相同欄位。
- 不要為假設性的手動修改、未指定的外部輸入或不可能狀態，在各層加入無實益的防禦性阻擋。
- 下游程式可依賴上游已驗證的不變量；只有該層確實是未信任輸入邊界，或驗證能避免明確且已定義的風險時，才新增驗證。

## 專案架構邊界

- `crates/game-core`：核心規則，只依賴 `bevy_ecs`、Serde 與 TOML；Godot 不參與規則運算。
- `crates/godot-bridge`：薄 GDExtension，將 JSON command 交給核心，並回傳 presentation snapshot。
- Godot 的 `features/battle`：僅負責輸入、繪圖和 UI。

### Godot 禁止承載遊戲規則

- 凡是根據遊戲資料進行計算、比較、合法性判斷或結果分類，一律由 `game-core` 負責；不因用途是執行、預覽或顯示而例外。
- Godot 只處理輸入、純介面狀態，以及核心已決定資料的排版、繪製與效果；不得從 snapshot 原始值重建規則。
- Godot 需要衍生資訊時，先擴充 `game-core` 的 command、query 或 presentation snapshot；`godot-bridge` 只轉換輸入輸出，不得解讀或補算規則。
- 修改 GDScript 前必須檢查上述邊界；發現規則推導時，先移回 `game-core`。

## 暫存檔案

- 需要建立暫存檔案或暫存目錄時，只能寫入專案根目錄下的 `ignore-tmp/`。
- 不得將本專案工作產生的暫存內容寫入系統暫存目錄、使用者目錄或專案外的其他位置。

## Rust

- 不使用 `let else` 語法。

### 結構解構

- 對函式回傳、Resource 快照或其他需要隨型別演進同步檢查的具名 struct，呼叫端優先使用完整解構取值，不要先綁定整個 struct 再逐欄位存取。
- 完整解構不得使用 `..`；不需要的欄位以 `欄位名: _` 明確列出。這能讓新增欄位時產生編譯錯誤，強制檢查並更新所有相關使用端。

### ECS 資料模型

- ECS 的 Component 與 Resource 僅能承載資料，禁止實作 method。

## Godot

### 實作前強制查詢

凡是要實作、修改或除錯 Godot 相關功能，必須先查閱相關的官方文件或其他第一方資料，再開始修改檔案。

查詢內容至少應涵蓋本次實作會使用的引擎 API、設定、場景、訊號、翻譯、輸入或輸出行為；不得只依賴記憶或推測。

開始修改前，必須向使用者簡述查詢到的相關依據、它如何影響實作，以及預計修改的檔案。只有規格或使用者可見行為仍有多種合理解讀時，才詢問使用者；純技術方案依本文件的「實作前確認」原則自行決定。

### 檔案組織

本專案 Godot 預設採用功能式組織：場景、控制腳本與功能專屬資源放在同一個功能資料夾；共用翻譯、資料與第三方資源放在全域資料夾。

新增或搬移會影響 Godot 檔案結構的檔案時，必須說明其放置位置與理由。

同一個場景是否拆成 `.tscn` 與 `.gd` 也必須說明理由。一般原則如下：

- `.tscn`：節點樹、版面與靜態訊號連線。
- `.gd`：行為、狀態與事件處理。
- 極小或完全動態建立的 UI，才考慮只使用 `.gd`。

### 測試替身

- Godot 測試禁止使用 mock 類別、節點或服務；只允許使用專用 TOML 測試資料，並以真實場景、實際類別與全域服務完成驗證。

### AI 呼叫 Godot Headless

Godot 4.7.1 在 Codex 沙箱內無法使用預設日誌位置，或 `--log-file` 指向不存在的目錄時，可能以 signal 11（`0xc0000005`）崩潰。Godot 的相對日誌路徑以 `project.godot` 所在目錄為基準，因此不得使用 `--log-file tmp/...`；也不要以 `--disable-file-logging` 迴避。

Headless 驗證應將日誌寫到工作區根目錄既有 `ignore-tmp/` 的絕對路徑。此處的相對路徑是 `ignore-tmp/`（相對於本 `AGENTS.md` 所在目錄），不是 `godot/ignore-tmp/`。

需要實際視窗、渲染或桌面輸入時，不使用 `--headless`，並在取得核准後於沙箱外啟動。參考：[Godot 命令列文件](https://docs.godotengine.org/en/stable/tutorials/editor/command_line_tutorial.html)、[Godot issue #118354](https://github.com/godotengine/godot/issues/118354)。
