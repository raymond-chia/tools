# 專案協作規則

## 語言規則

- 程式碼註解、文件說明與協作回覆優先使用繁體中文。

## 實作前確認

當需求存在兩個以上合理實作方式，或選擇會影響 API、資料格式、架構、檔案範圍、相容性、效能或使用者可見行為時，修改檔案前必須：

1. 先檢查現有專案結構、慣例與相關檔案。
2. 列出主要方案與取捨。
3. 明確指出建議方案及理由。
4. 說明假設、不處理的範圍與可能風險。
5. 明確討論檔案放置位置、檔案是否拆分，以及各檔案的責任。
6. 等使用者確認方案後才修改檔案。

只有在需求已明確指定做法，或不同做法不會造成可見影響時，才可直接實作。

## 實作範圍

- 不要為未指定的未來需求增加相容性、格式支援或防禦性分支。
- 只依照目前明確定義的格式與行為實作。

## 外部查詢與技術依據

當需求涉及可能變動、版本相依、引擎／框架行為，或對既有行為沒有把握時，實作前必須查詢官方文件或其他第一方資料，不得只依記憶推測。

查詢結果只用於當次任務的判斷，不要把整份外部文件或逐次查詢內容複製到本檔案。完成任務時，只在回覆中簡述真正影響實作的依據與來源；不要在 `AGENTS.md` 累積查詢紀錄或固定過時版本結論。

## 專案功能索引

- 開始實作前，先閱讀 `.claude/rules/core-index.md`，優先依照其中的功能索引定位相關模組與檔案；只有索引沒有涵蓋或內容不足時，才進一步使用全文搜尋。

## Godot

## 核心 ECS 與 Godot FFI 責任邊界

- `godot_bind/` 的 ECS API 僅負責 Godot 型別、結果與錯誤格式轉換，不重複實作 `core/board` 的邏輯與驗證。
- Resource 查詢優先使用 `query::get_resource`；以 `.is_ok()`／`.is_err()` 判斷是否存在／缺失。
- FFI 層若新增核心沒有的限制，須明確標示為 FFI 政策。

### Godot 實作前強制查詢

凡是要實作、修改或除錯 Godot 相關功能，必須先查閱相關的官方文件或其他第一方資料，再開始修改檔案。

查詢內容至少應涵蓋本次實作會使用的引擎 API、設定、場景、訊號、翻譯、輸入或輸出行為；不得只依賴記憶或推測。

開始修改前，必須向使用者簡述查詢到的相關依據、它如何影響實作，以及預計修改的檔案。若需求存在多種合理方案，仍須先列出方案與取捨，等待使用者確認後才能修改。

### Godot 檔案組織

本專案 Godot 預設採用功能式組織：場景、控制腳本與功能專屬資源放在同一個功能資料夾。共用翻譯、資料與第三方資源放在全域資料夾。

當新增或搬移會影響 Godot 檔案結構的檔案時，必須說明其放置位置與理由。

同一個場景是否拆成 `.tscn` 與 `.gd` 也必須說明理由。一般原則是：

- `.tscn`：節點樹、版面與靜態訊號連線。
- `.gd`：行為、狀態與事件處理。
- 極小或完全動態建立的 UI，才考慮只使用 `.gd`。

### Godot Git 範圍

- Godot 專案的 Git 檢查與操作限定在 `godot/` 目錄內。

### Godot 專案規則檔

- 處理 `godot/` 或 `godot_bind/` 相關功能前，必須先閱讀「相對於專案根目錄」的 `.claude/rules/godot.md`。
