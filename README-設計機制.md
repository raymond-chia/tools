# 遊戲設計機制文檔

## 核心願景

### 你希望遊戲的核心樂趣是什麼？

- 規劃戰術, 逐一嘗試戰術是否有效。最終發現戰術能穩定成功時會有成就感
- 需要有某種程度的隨機性, 以免像是在下棋, 並產生最優解
- 不能隨機出現敵人, 以免難以設計戰術
- 需要根據喜好戰術培養角色, 讓不同角色使用不同技能合作達成戰術

- 比方來說 pathfinder wrath of the righteous 的 through the ashes DLC 中, 會需要利用地形甚至機關來對付敵人
- 需要針對性培養角色, 才好應付無法依靠地利對付的敵人

### 你希望玩家在戰鬥中主要做什麼決策？

- 主要是根據戰術施放技能, 一步一步完成戰術
- 戰術需要考量資源分配, 比如法術會消耗 MP
- 戰術可能包含位置, 比如把敵人推下懸崖, 或者堵住隘口讓後排盡情輸出

---

## 遊戲定位

**類型**：戰術規劃 + 試錯 + 協同戰鬥 + 地形利用的回合制戰棋 RPG

**參考遊戲**：

- Pathfinder: Wrath of the Righteous (地形與機關戰術)
- Divinity: Original Sin 2 (元素互動與地形利用)
- Into the Breach (完美信息與位置戰術)
- XCOM 2 (資源管理與風險評估)
- 火焰之紋章 (地形系統與角色養成)

**核心循環**：

1. 觀察戰場（敵人位置、地形、可用資源）
2. 規劃戰術（技能組合、位置利用、資源分配）
3. 執行戰術（移動、施法、團隊合作、利用地形）
4. 評估結果（成功 → 下一關 / 失敗 → 調整戰術重試）

**核心理念**：

> **針對性的策略應該能克服隨機性。**
>
> 隨機性只是讓高風險高報酬的戰術不易實現，但不會完全不可能實現。
> 玩家可以針對性地通過裝備、技能、位置等方式提高成功率，將「賭博式戰術」變成「高成功率戰術」。

---

## 當前設計問題診斷

### 缺失的關鍵要素

#### 1. 地形互動系統不足

**問題**：

- 雖然有 `Shove` 效果（推擊），但沒有**深淵**等致命地形
- 沒有**阻擋視線**的地形（掩體、牆壁）
- 沒有**可互動物件**（機關、陷阱、可破壞物）

**影響**：

- 無法實現「推下懸崖」、「堵住隘口」等戰術
- 缺少地形帶來的戰術深度

#### 2. 技能協同機制不明顯

**問題**：

- 沒有「狀態 + 技能」的聯動（例如：油地 + 火球 = 範圍燃燒）
- 沒有「角色 + 角色」的配合（例如：隊友包夾 → 夾擊傷害加成）

**影響**：

- 無法實現「角色使用不同技能合作達成戰術」
- 技能只是單獨使用，缺乏協同樂趣

#### 2. 資源管理層次不足

**問題**：

- MP 目前只是每場戰鬥的資源
- 沒有跨戰鬥的資源管理（MP 應該是一天的限制）
- 沒有消耗品系統（藥水、卷軸）

**影響**：

- 資源管理決策過於簡單
- 缺少「謹慎使用大招」的緊張感
- 無法實現「連續多場戰鬥」的關卡設計

#### 3. 缺少風險回報的選擇

**問題**：

- 技能設計可能缺少「穩健」vs「賭博」的選擇
- 缺少讓玩家「克服隨機性」的機制（buff、裝備等）

**影響**：

- 無法實現「針對性策略克服隨機性」的理念
- 戰術規劃深度不足

---

## 核心機制設計

### 機制 1：地形系統（已有基礎，需擴充）

#### 目前的設計（很好，保持）

```rust
pub struct Tile {
    pub terrain: Terrain,      // 基礎地形
    pub object: Option<Object>, // 地形物件
}

// Terrain：影響移動成本
pub enum Terrain {
    Plain,        // 平原，移動成本 10
    Hill,         // 丘陵，移動成本 13
    Mountain,     // 山地，移動成本 20
    Forest,       // 林地，移動成本 13
    ShallowWater, // 淺水，移動成本 17
    DeepWater,    // 深水，移動成本 999（不可通行）
}

// Object：影響通行性和戰術
pub enum Object {
    Wall,         // 牆壁，不可通行
    Tree,         // 樹木，不可通行
    Cliff { orientation: Orientation }, // 懸崖邊緣
    // 需要新增 ↓
    Pit,          // 深淵，掉落即死
    // 未來可擴充 ↓
    // Barrel,    // 油桶，可破壞
    // Lever,     // 拉桿，觸發機關
}
```

**設計優點**：

- ✅ Terrain 和 Object 分離清晰
- ✅ 可以組合（例如：山地上的牆壁）
- ✅ 邏輯簡單，容易擴充
- ✅ 不需要複雜的高度系統

#### 需要加入的功能

**1. Pit（深淵）物件**

```rust
pub enum Object {
    Wall,
    Tree,
    Cliff { orientation: Orientation },
    Pit,  // 新增：深淵/深坑，掉落即死
    Tent2 { ... },
    Tent15 { ... },
}

impl Object {
    pub fn is_passable(&self) -> bool {
        match self {
            Object::Wall
            | Object::Tree
            | Object::Cliff { .. }
            | Object::Pit  // 深淵不可通行
            | Object::Tent2 { .. }
            | Object::Tent15 { .. } => false,
        }
    }
}
```

**2. Shove 技能檢測掉落**

```rust
Effect::Shove { distance, .. } => {
    // 計算推擊方向
    let direction = calc_direction(caster_pos, target_pos);

    // 計算推擊後的位置
    let new_pos = calc_new_pos(target_pos, direction, distance);

    // 檢查推擊目標位置
    if let Some(tile) = board.get_tile(new_pos) {
        // 關鍵：檢查是否推入深淵
        if tile.object == Some(Object::Pit) {
            // 掉落即死
            target_unit.hp = 0;
            return Some("被推入深淵，墜落身亡！");
        }

        // 正常推擊邏輯
        // ...
    }
}
```

#### 地形效果範例

| 地形類型             | 移動消耗 | 效果                 | 戰術用途         |
| -------------------- | -------- | -------------------- | ---------------- |
| Plain（平原）        | 10       | 無                   | 快速移動         |
| Forest（林地）       | 13       | 無                   | 減緩敵人追擊     |
| Mountain（山地）     | 20       | 無                   | 阻擋路線         |
| ShallowWater（淺水） | 17       | 未來可加「潮濕」狀態 | 元素協同（暫緩） |
| DeepWater（深水）    | 999      | 不可通行             | 地形障礙         |

| 物件類型        | 通行性   | 效果         | 戰術用途       |
| --------------- | -------- | ------------ | -------------- |
| Wall（牆壁）    | 不可通行 | 阻擋視線     | 掩體、堵路     |
| Tree（樹木）    | 不可通行 | 無           | 地形障礙       |
| Cliff（懸崖邊） | 可通行   | 標記懸崖位置 | 推擊戰術的起點 |
| Pit（深淵）     | 不可通行 | 掉落即死     | 推擊戰術的終點 |

#### 地形戰術範例

**戰術 1：推下懸崖**

```
佈局：
  敵人 站在 Cliff（懸崖邊）
  Cliff 旁邊是 Pit（深淵）

執行：
  1. 戰士移動到敵人旁邊
  2. 使用「Shove」技能推擊
  3. 敵人被推入 Pit
  4. 掉落即死

結果：
  - 即使是高血量 Boss，也能一擊必殺
  - 風險：需要敵人站在懸崖邊
```

**戰術 2：堵住隘口**

```
佈局：
  山谷地形，只有一條通道（兩側是 Wall）

執行：
  1. 坦克站在通道口
  2. 敵人無法繞過（Wall 不可通行）
  3. 後排安全輸出

結果：
  - 以少敵多的關鍵戰術
  - 地形成為戰術的一部分
```

**戰術 3：利用困難地形**

```
佈局：
  我方在 Plain（移動成本 10）
  敵人在 Mountain（移動成本 20）

執行：
  1. 射手在 Plain 上移動並攻擊
  2. 敵人在 Mountain 上追不上（移動慢）
  3. 風箏戰術

結果：
  - 移動力優勢轉化為戰術優勢
```

---

### 機制 2：命中與隨機性系統（已有，需完善）

#### 目前的命中系統（很好，保持）

你目前的百分比系統（不是骰子！）：

```rust
// 1. 擲 1-100 的隨機數
let hit_random = rng.random_range(1..=100);

// 2. 計算命中分數
let hit_score = accuracy + hit_random;

// 3. 判定
if hit_random <= 5 {
    完全閃避（大失敗，5%）
} else if hit_random > 95 {
    完全命中（大成功，5%）
} else {
    // 計算閃避
    let evade_score = hit_score - evasion;
    if evade_score <= 0 {
        閃避
    } else if hit_score - evasion - block <= 0 {
        格擋（減傷）
    } else {
        命中
    }
}
```

**這個系統的優點**：

- ✅ 直觀：不需要解釋「d20」「d6」等術語
- ✅ 可預測：玩家可以看到 accuracy、evasion、block 數值
- ✅ 有層次：大成功/大失敗、閃避、格擋、命中
- ✅ 符合「針對性策略克服隨機性」的理念

**閃避與格擋疊加的設計理念**：

閃避和格擋採用**疊加判定**（而非分離判定）：
```
閃避區間: hit_score ≤ evasion          → 完全避免傷害
格擋區間: evasion < hit_score ≤ evasion + block → 減少傷害（第二道防線）
命中區間: hit_score > evasion + block     → 完整傷害
```

**為什麼採用疊加設計？**

1. **鼓勵多元投資**：如果分離判定，玩家會只投資閃避或格擋其中一種（選擇報酬率更高的）。疊加設計讓兩者都有價值：
   - 閃避：第一道防線，完全避免傷害
   - 格擋：第二道防線，當閃避失敗時減傷

2. **創造攻防對抗**：高防禦可被高 accuracy 破解，產生策略深度：
   - 防禦方：提升 evasion + block 創造難以命中的區間
   - 攻擊方：提升 accuracy 或使用高命中技能破防

3. **平衡性**：疊加不會導致無敵，因為攻擊者可以：
   - 提升自身 accuracy（永久提升）
   - 使用高 accuracy 技能（針對性破防）
   - 搭配 buff 技能提高命中

#### 傷害系統建議

**選項 A：固定傷害（目前）**

```rust
Effect::Hp { value: -10, ... }  // 固定 10 傷害
```

- 優點：完全可預測
- 缺點：缺少隨機性

**選項 B：範圍傷害（推薦）**

```rust
// 修改 Effect
pub enum Effect {
    Hp {
        target_type: TargetType,
        shape: Shape,
        min_value: i32,  // 最小傷害
        max_value: i32,  // 最大傷害
    },
    // ...
}

// 套用效果時
let actual_damage = rng.random_range(min_value..=max_value);
unit.hp += actual_damage;
```

**範例**：

```rust
// 普通攻擊：5-10 傷害
Effect::Hp {
    min_value: -10,
    max_value: -5,
    ...
}

// 火球術：15-25 傷害
Effect::Hp {
    min_value: -25,
    max_value: -15,
    ...
}

// 治療術：8-12 恢復
Effect::Hp {
    min_value: 8,
    max_value: 12,
    ...
}
```

**優點**：

- ✅ 不需要骰子術語
- ✅ 玩家看到「5-10 傷害」很直觀
- ✅ 保留隨機性，但容易理解
- ✅ 實作簡單：`random_range(min, max)`

#### 技能設計範例：穩健 vs 賭博

**關鍵設計原則**：每個角色都要有「穩健技能」和「賭博技能」

```rust
// 戰士技能組
pub const WARRIOR_SKILLS: &[&str] = &[
    "slash",          // 穩健：80% 命中，5-8 傷害
    "heavy_strike",   // 平衡：60% 命中，10-15 傷害
    "berserk_slash",  // 賭博：40% 命中，20-30 傷害
];

// 法師技能組
pub const MAGE_SKILLS: &[&str] = &[
    "magic_missile",  // 穩健：100% 命中（必中），3-5 傷害/發
    "fireball",       // 平衡：70% 命中，15-20 傷害 AOE
    "meteor",         // 賭博：50% 命中，30-40 傷害 AOE
];
```

**設計表格**：

| 技能類型 | 命中率  | 傷害 | MP 消耗 | 設計意圖         |
| -------- | ------- | ---- | ------- | ---------------- |
| 穩健技能 | 80-100% | 低   | 少/無   | 克服隨機性的選項 |
| 平衡技能 | 60-80%  | 中   | 中      | 日常使用         |
| 賭博技能 | 40-60%  | 高   | 高      | 高風險高回報     |

#### 「針對性策略克服隨機性」範例

**情境**：Boss 戰，Boss 有高閃避（evasion = 50）

```
策略 A：直接攻擊（隨機）
    使用「重擊」(accuracy = 30)
    hit_score = 30 + random(1-100) = 31-130
    evade_score = hit_score - 50 = -19 到 80
    → 成功率約 60%（不穩定）

策略 B：針對性策略（克服隨機性）
    1. 使用「祝福術」給自己 +20 accuracy
    2. 裝備「精準項鍊」+10 accuracy
    3. 利用「高地優勢」+10 accuracy（未來擴充）

    現在 accuracy = 70
    evade_score = 70 + random(1-100) - 50 = 21-120
    → 成功率約 95%（穩定！）

策略 C：放棄攻擊，利用地形（完全克服隨機性）
    1. 把 Boss 引到懸崖邊
    2. 使用「Shove」推下去
    → 成功率 100%（如果能引到懸崖邊）
```

**關鍵**：

- ✅ 穩健戰術必定存在（策略 B、C）
- ✅ 玩家可以選擇承受多少風險
- ✅ 針對性準備能克服隨機性

---

### 機制 3：資源管理系統（MP 是一天的限制）

#### 設計目標

- MP 是「一天的資源」，不是「每場戰鬥的資源」
- 玩家需要規劃「哪一場戰鬥用大招」
- 強力技能消耗大量 MP，需要謹慎使用

#### 實作建議

```rust
pub struct Unit {
    // ... 現有欄位 ...

    // MP 是「一天的資源」
    pub mp: i32,        // 當前 MP
    pub max_mp: i32,    // 最大 MP
    // 戰鬥結束後，MP 不會恢復！
    // 只有「長休息」（回營地、過一天）才會恢復
}

// 技能 MP 消耗設計
pub const SKILLS: &[(&str, i32)] = &[
    ("普通攻擊", 0),      // 免費，無限使用
    ("重擊", 5),          // 消耗 5 MP
    ("火球術", 15),       // 消耗 15 MP（強力 AOE）
    ("治療術", 10),       // 消耗 10 MP
    ("大召喚術", 30),     // 消耗 30 MP（終極技能）
];
```

#### 長休息機制

```rust
// 戰鬥結束後
pub fn end_battle(board: &mut Board) {
    // MP 不恢復
    // HP 可能部分恢復，或需要使用道具
}

// 長休息（回營地、過一天）
pub fn long_rest(board: &mut Board) {
    for unit in board.units.values_mut() {
        unit.mp = unit.max_mp;  // MP 恢復到滿
        unit.hp = unit.max_hp;  // HP 恢復到滿
    }
}
```

#### 戰術規劃範例

**情境**：一天有 3 場戰鬥，法師 MP 上限 50

```
戰鬥 1（雜兵戰）：
    選擇 A：用「火球術」(15 MP) 快速清場
        → 剩餘 MP: 35
        → 優點：快速，減少受傷
        → 缺點：後面戰鬥可能不夠用

    選擇 B：用「普通攻擊」慢慢打
        → 剩餘 MP: 50
        → 優點：保留 MP 給後面
        → 缺點：可能受傷，消耗藥水

戰鬥 2（精英怪）：
    如果選 A：只剩 35 MP
        → 可以用 2 次「火球術」(30 MP) + 普通攻擊
        → 還剩 5 MP

    如果選 B：還有 50 MP
        → 可以用「大召喚術」(30 MP) 輕鬆打
        → 還剩 20 MP

戰鬥 3（Boss 戰）：
    如果選 A：只剩 5 MP
        → 只能用「重擊」(5 MP) 一次，其餘用普通攻擊
        → 困難！可能需要消耗藥水

    如果選 B：還有 20 MP
        → 可以用「火球術」(15 MP) + 「重擊」(5 MP)
        → 比較容易
```

**這就是「謹慎使用 MP」的資源管理決策！**

#### 消耗品系統（未來擴充）

```rust
pub struct Consumable {
    pub id: String,
    pub name: String,
    pub effect: ConsumableEffect,
    pub quantity: u8,
}

pub enum ConsumableEffect {
    RestoreMp(i32),       // MP 藥水
    RestoreHp(i32),       // HP 藥水
    BuffAccuracy(i32),    // 命中藥水
    // ...
}
```

**設計意圖**：

- MP 藥水很珍貴（稀有、昂貴）
- 玩家需要決定「什麼時候用藥水」
- 跨戰鬥的戰略性資源管理

---

## 規則系統建議

### ❌ 不要做的事

**1. 不要直接套用 PF2E 或 DND 規則**

原因：

- **版權風險**：WotC 對規則有版權保護
- **過於複雜**：PF2E Remaster 有上千條規則，小團隊無法實作
- **不適合電子遊戲**：這些規則為桌遊設計，很多機制在電子遊戲中體驗不佳
- **缺乏差異化**：你的遊戲會變成「劣質版 PF2E」

**2. 不要使用骰子術語（d20, d6 等）**

原因：

- 不是所有玩家都熟悉桌遊術語
- 百分比和範圍（5-10）更直觀
- 你的系統已經用百分比，不需要改成骰子

**3. 不要追求數值平衡（初期）**

原因：

- 先追求「有趣的戰術」，再調整平衡
- 不平衡的機制反而能創造獨特戰術（例如：推下懸崖是「不平衡」的，但很有趣）

**4. 不要做複雜的高度系統**

原因：

- 實作複雜，邏輯難懂
- Terrain + Object 的分離已經足夠
- 掉落傷害計算沒必要，直接「掉落即死」更有戰術意義

### ✅ 應該做的事

**1. 借鑒精神，不抄規則**

從 PF2E/DND 學習的**設計理念**（非具體數值）：

| PF2E/DND 機制 | 設計理念         | 你的實作                    |
| ------------- | ---------------- | --------------------------- |
| 先攻骰        | 回合順序有隨機性 | 已實作：initiative + 1d6    |
| AC 與攻擊骰   | 命中機制         | 已實作：accuracy vs evasion |
| 狀態條件      | 豐富的戰術選擇   | 已有部分（Burn 等），可擴充 |
| 法術位        | 資源管理         | MP 作為一天的限制           |
| 地形規則      | 戰術深度         | Terrain + Object 系統       |

**2. 學習優秀遊戲的設計**

| 遊戲                     | 學習要點           | 應用到你的遊戲             |
| ------------------------ | ------------------ | -------------------------- |
| Divinity: Original Sin 2 | 元素互動、地形戰術 | 狀態協同系統、地形互動     |
| Into the Breach          | 完美信息、位置戰術 | 推擊戰術                   |
| XCOM 2                   | 風險評估、資源管理 | 穩健 vs 賭博技能、MP 管理  |
| 火焰之紋章               | 地形系統、角色養成 | Terrain 影響移動、單位成長 |
| Darkest Dungeon          | 跨戰鬥資源管理     | MP 是一天的限制            |

**3. 保持簡單，逐步擴充**

**優先級 1（立即做）**：

- ✅ 加入 Pit 物件
- ✅ 修改 Shove 技能檢測掉落
- ✅ 設計 1 個懸崖戰鬥關卡

**優先級 2（接下來做）**：

- ✅ MP 改成「一天的限制」（戰鬥結束不恢復）
- ✅ 設計連續 2-3 場戰鬥的關卡
- ✅ 實作長休息機制

**優先級 3（可選）**：

- 傷害改成範圍（min-max）
- 加入 buff 技能（提高 accuracy）
- 加入消耗品系統（MP 藥水）

**暫緩實作**：

- 地圖上有可互動的機關，玩家可以利用（類似 Divinity）
- 技能可以改變地形（造水、造冰、炸開牆壁等）
- 複雜的技能協同系統（潮濕+冰凍等）
- 可互動物件（油桶、拉桿等）
- 高度系統、視線系統

---

## 實作優先級

### 第一階段：地形戰術基礎（立即可做）

**目標**：讓「推下懸崖」戰術可玩

**步驟**：

1. 在 `Object` 中加入 `Pit` 變體
2. 修改 `Object::is_passable()`，Pit 不可通行
3. 在 `apply_effect_to_pos` 或新增函數中，處理 `Effect::Shove`：
   - 計算推擊方向和目標位置
   - 檢查目標位置是否為 `Pit`
   - 如果是 `Pit`，目標單位 `hp = 0`
4. 在 editor 中設計測試關卡：
   - 地圖有 Cliff（懸崖邊）和 Pit（深淵）
   - 敵人初始位置在 Cliff 附近
   - 玩家需要用 Shove 推敵人入 Pit

**預期成果**：

- 玩家可以把敵人推下懸崖
- 感受到「利用地形的成就感」
- 驗證「針對性策略克服隨機性」的理念

**測試關卡設計範例**：

```
關卡：懸崖邊的哥布林

佈局：
  P = Plain
  C = Cliff
  X = Pit
  G = Goblin (敵人)
  W = Warrior (玩家)

  P P P P P
  P W P G C
  P P P P X
  P P P P X

目標：
  - 擊殺所有敵人

戰術：
  方案 A（直接攻擊）：
    - 需要攻擊 3-4 次才能殺死哥布林
    - 可能受傷

  方案 B（推下懸崖）：
    - 移動到哥布林旁邊
    - 使用 Shove 推向 Pit
    - 一擊必殺
```

### 第二階段：MP 資源管理系統

**目標**：讓「資源分配決策」有意義

**步驟**：

1. 修改戰鬥結束邏輯，MP 不恢復
2. 實作 `long_rest()` 函數，恢復所有 MP
3. 在 editor 中設計連續戰鬥關卡：
   - 關卡 1：雜兵戰（可選擇省 MP 或快速清場）
   - 關卡 2：精英怪（中等難度）
   - 關卡 3：Boss 戰（需要保留 MP）
   - 三場戰鬥之間不能長休息
4. 調整技能 MP 消耗：
   - 普通攻擊：0 MP
   - 強力技能：10-30 MP
   - 確保玩家需要「謹慎使用大招」

**預期成果**：

- 玩家需要規劃「哪一場用大招」
- 感受到「資源管理的緊張感」
- 戰術規劃需要考慮跨戰鬥的資源

### 第三階段：傷害範圍與風險回報

**目標**：加入「穩健 vs 賭博」的選擇

**步驟**：

1. 修改 `Effect::Hp`，加入 `min_value` 和 `max_value`
2. 套用效果時，使用 `random_range(min, max)`
3. 設計技能：
   - 每個角色有「穩健技能」（高命中低傷害）
   - 每個角色有「賭博技能」（低命中高傷害）
4. UI 顯示：
   - 「普通攻擊：80% 命中，5-10 傷害」
   - 「狂暴斬擊：40% 命中，20-30 傷害」

**預期成果**：

- 玩家可以根據情況選擇「穩健」或「賭博」
- 失敗時可以重試，調整戰術
- 驗證「隨機性不破壞戰術規劃」的理念

### 第四階段：buff 與「克服隨機性」

**目標**：讓玩家能「克服隨機性」

**步驟**：

1. 設計 buff 技能（提高 accuracy）
2. 設計 buff 效果的持續時間
3. 設計消耗品（命中藥水、MP 藥水）
4. 設計裝備系統（精準項鍊 +10 accuracy）

**預期成果**：

- 玩家可以通過 buff、裝備、消耗品提高成功率
- 「賭博技能」變成「高成功率技能」
- 真正實現「針對性策略克服隨機性」

### 第五階段：角色養成與長期目標

**目標**：讓「培養角色」有意義

**步驟**：

1. 設計技能學習系統（升級獲得技能點）
2. 設計裝備系統（影響屬性）
3. 設計長期戰役模式（多個關卡，角色持續成長）

**預期成果**：

- 玩家可以根據喜好戰術培養角色
- 不同玩家會有不同的角色構建和戰術

---

## 總結

### 你的遊戲核心

**一句話概括**：

> 戰術規劃 + 地形利用 + 資源管理的回合制戰棋 RPG

**核心樂趣循環**：

1. 觀察戰場（地形、敵人、資源）
2. 規劃戰術（技能組合、位置利用）
3. 執行戰術（移動、施法、利用地形）
4. 評估結果（成功 → 成就感 / 失敗 → 調整重試）

**核心理念**：

> **針對性的策略應該能克服隨機性。**

### 必須實作的核心系統

1. ✅ **地形系統**：Pit（深淵）+ Shove（推擊）= 推下懸崖戰術
2. ✅ **命中系統**：百分比系統（不是骰子），可預測的隨機性
3. ✅ **資源管理**：MP 是一天的限制，謹慎使用大招
4. ✅ **風險回報**：穩健技能 vs 賭博技能，玩家可選擇

### 設計哲學

**要做**：

- ✅ 保持簡單，逐步擴充
- ✅ 先追求有趣的戰術，再調整平衡
- ✅ 借鑒優秀遊戲的設計理念（不是抄規則）
- ✅ 確保「穩健戰術」必定存在

**不要做**：

- ❌ 直接抄襲 PF2E/DND 規則（版權 + 複雜度）
- ❌ 使用骰子術語（d20, d6）
- ❌ 做複雜的高度系統
- ❌ 過早追求完美平衡

### 下一步行動

1. **立即開始**：實作 Pit 物件和 Shove 掉落邏輯
2. **接下來**：設計懸崖戰鬥測試關卡
3. **然後**：實作 MP 跨戰鬥資源管理
4. **測試**：找玩家測試，看戰術規劃是否有趣
5. **迭代**：根據反饋調整機制

記住：**好玩比完整更重要**。先把核心循環做得有趣，再擴充內容。
